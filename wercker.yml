box: golang:latest

build:
  steps:
    - arjen/hugo-build:
        theme: academic
        version: "HEAD"

deploy:
  steps:
    - install-packages:
            packages: git
    - leipert/git-push:
            gh_oauth: $GIT_TOKEN
            repo: pat-s/pat-s.github.io
            branch: master
            basedir: public
